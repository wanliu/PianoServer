<div class="col-xs-4 col-sm-4 col-md-3 cate" >
  <a href="{{shop.link}}{{category.link_html}}" class="text-center logo-wrap">
    {% unless category.blank_image %}
      {{ category.cover_url | image_tag }}
    {% else %}
      {{ 'trans_blank.gif' | image_url | image_tag }}
      <div class="cate-desc">{{ category.title }}</div>
    {% endunless %}
  </a>
  <div class="bottom-shadows"></div>
<!--   <div class="text-center cate-title">{{ category.title }}</div>
  {% comment %}<span class='text text-center'>{{ category.title }}</span>{% endcomment %} -->
</div>
