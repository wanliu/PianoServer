<style>

  body.shop .main-container{
    background: #02050E;
  }

  body.shop .return-btn {
    right: 21px;
  }
  
  body.shop .return-btn a{
    background: #5F99ED;
  }
  body.shop .return-btn a:before{
    border-right-color: #5F99ED;
  }

  body.shop footer *{
    color: #fff;
  }
  body.shop .brand-board .shop-info{
    position: relative;
    border-bottom: 2px dashed #5D99E9;
  }
  body.shop .brand-board .shop-info .name-address .name{
    font-size: 26px;
  }
  body.shop .brand-board .shop-info{
    padding: 21px;
    background: #152E4E;
  }

  body.shop .brand-board .shop-info .shop-logo-wrapper{
    padding: 0;
    width: 90px;
  }

  body.shop .brand-board .shop-info .shop-logo{
    width: 100%;
    height: auto;
  }

  body.shop .brand-board .shop-info .attention{
    border: 2px solid #000;
    /*background-color: #0D1D3C;*/
    background: -webkit-radial-gradient(#183669, #1B3465, #0D1D3C);

  }

  body.shop .brand-board .shop-info .name-address{
    padding-left: 20px;
  }

  body.shop .shop-categories{
    background-color: #26426A;
    padding: 0 20px;
  }

  body.shop .shop-categories *{
    transition: all 0.6s;
    -webkit-transition: all 0.6s;
  }

  body.shop .title a{
    color: #fff;
  }
  body.shop .title{
    margin: 0;
    padding: 0 20px;
    background-color: #25426B;
  }
  body.shop .cate .mask{
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,.5);
    left: 0;
    top: 0;
  }  
  body.shop .cate a{
    border-radius: 0;
    border: 2px dashed #5D99E9;
    background: transparent;
    padding: 5px;
    position: relative;
  }
  body.shop .cate a img{
    width: 100%;
    border: 1px solid transparent;
  }

  body.shop .cate-title-wrap{
    position: absolute;
    width: 100%;
    top: 66%;
    left: 0;
  }
  body.shop .cate-title-wrap .cate-title{
    display: inline-block;
    background-color: #0A0D16;
    color: #fff;
    padding: 10px;
    font-size: 16px;
  }
  body.shop .cate a:hover{
    border-color: #FDB567;
  }

  body.shop .cate a:hover img{
    border: 1px solid #FDB567;
  }

  body.shop .cate a:hover .mask{
    background: transparent;
  }

  body.shop .cate a:hover .cate-title{
    background-color: #5F99ED
  }

  body.shop .shop-items{
    background: #25426B;
    padding: 0 20px;
  }

  body.shop .shop-items .promotion{
    border: 2px dashed #5D99E9;
    background: transparent;
  }

  body.shop .shop-items .promotion-image-wrap{
    border-bottom: 2px dashed #5D99E9;
  }

  body.shop .promotion-title{
    padding: 0;
    color: #fff;
  }

  body.shop .promotion .price-board{
    background: #5F99ED;
  }

  body.shop .shop-items .cate-item *{
    transition: all 0.6s;    
    -webkit-transition: all 0.6s;    
  }

  body.shop .shop-items .promotion:hover{
    border: 2px dashed #FDB567;
  }
  body.shop .shop-items .promotion:hover .price-board{
    background: #F58A44;
  }

  body.shop .shop-items .promotion:hover .promotion-image-wrap{
    border-color: #FDB567;
  }

  body.shop .shop-items .promotion .prices{
    padding: 0;
    font-size: 16px;
  }

  .pagination > li a,
  .pagination > li span{
    border-radius: 0!important;
    margin: 3px;
    border: none;
    background: #5F99ED;
    color: #fff;
  }

  .pagination > .active > span{
    background: #F58A44;
  }

  @media (max-width: 484px){
    body.shop .brand-board .shop-info .shop-logo-wrapper{
      width: 60px;
    }
  }
</style>