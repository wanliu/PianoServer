<a href="javascript:void(0);" class="list-group-item promotion" data-id="<%= promotion_item.id %>" >
  <h4>
    <span class="id"><%= promotion_item.id %></span>
    <span class="title"><%= promotion_item.title %></span>
  </h4>
</a>
