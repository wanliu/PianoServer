$(".toggle_inhibit")
  .attr("href", "<%= show_inhibit_admins_industry_category_path(@industry, @category) %>")
  .text("显示 (压制 <%= @category.inhibit_count %> ...)")
$(".property-list").html("<%=j render partial: "property", collection: @properties, locals: { category: @category, industry: @industry } %>")
