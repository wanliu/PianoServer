<%= render "form" %>

<!-- 运费设置 -->
<% if @item.persisted? %>
  <fieldset >
    <legend >运费模板设置</legend>
    <%= render partial: "express_template", locals: { item: @item } %>
  </fieldset>

  <fieldset >
    <legend >礼品设置</legend>
    <%= render partial: "gifts", locals: { item: @item } %>
  </fieldset>
<% end %>
