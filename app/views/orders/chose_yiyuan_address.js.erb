(function() {
  var html = "<%= j(render 'addresses.html.erb') %>";
  $('.address-zone').html(html).show();
  $('.order-confirm-mobile').hide();

  var state = {show: "address-zone", hide: "order-confirm-mobile"};
  history.pushState(state, "选择地址", "chose_address");
})();