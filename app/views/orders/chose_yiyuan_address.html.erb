<div class="congrat alert alert-success text-center" role="alert">
  恭喜你，抢购成功！请选择收货地址
</div>

<%= render "addresses" %>
<%#= link_to "管理我的收货地址", locations_path(callback: request.fullpath), class: 'pull-right' %>

<script type="text/javascript">
  $(document).on('ready page:load', function() {
    $('.yiyuan-address-item').click(function (event) {
      event.preventDefault();

      var addressId = $(this).data('addressId');
      var url = "<%= callback_url.try(:html_safe) %>";

      url = url.split('&address_id=')[0]
      url += ('&address_id=' + addressId);
      Turbolinks.visit(url);
      // window.location.href = url;
    })
  })
</script>